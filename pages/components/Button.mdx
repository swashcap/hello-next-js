import {Button} from '../../components/library/Button'
import {CodeBlock} from '../../components/styleguide/CodeBlock'
import {Grid} from '../../components/styleguide/Grid'
import {MethodsTable} from '../../components/styleguide/component/MethodsTable'
import {PropsTable} from '../../components/styleguide/component/PropsTable'
import {TableOfContents} from '../../components/styleguide/component/TableOfContents'

# Button

<Grid>
  <Grid.Column className="w-100 w-75-l">
    <p className="f4">The Button is the base interactive component for hello-next-js: it affords navigation, form submission, and application state changes.</p>
    <p>Three variations of Button exist to demonstrate the level of interaction: primary, secondary, and tertiary.</p>
    <CodeBlock language="tsx" preview scope={{Button}}>
      {`<div className="flex flex-wrap items-center content-center">
  <Button className="mr3">Primary</Button>
  <Button className="mr3" variant="secondary">Secondary</Button>
  <Button variant="tertiary">Tertiary</Button>
</div>`}
    </CodeBlock>
  </Grid.Column>
  <Grid.Column className="w-100 w-25-l">
    <TableOfContents
      items={[
        ['Usage', ['Variants']],
        ['Accessibility'],
        ['Component API', ['Props', 'Methods']]
      ]}
    />
  </Grid.Column>
</Grid>

#### Related

* [Interaction design guidelines ↗](#)
* [Button design specifications ↗](#)

## Usage

Import the `Button` component into a React component in your project:

```typescript
import {Button} from 'hello-next-js'
```

Use `Button` by passing it children and an `onClick` handler in your React component:

```tsx
import React from 'react'
import {Button} from 'hello-next-js'

export const MyComponent: React.FC<any> = () => {
  return (
    <Button
      onClick={(event) => {
        console.log('Clicked!', event)
      }}
    >
      My Button
    </Button>
  )
}
```

### Variants

`Button` comes with three variations, set through the `variant` property:

#### Primary

Use the primary variant for call to action, form submission, dialog or modal trigger.

<CodeBlock language="tsx" live scope={{Button}}>
  {`<Button>Primary button</Button>`}
</CodeBlock>

#### Secondary

<CodeBlock language="tsx" live scope={{Button}}>
  {`<Button variant="secondary">Secondary button</Button>`}
</CodeBlock>

#### Tertiary

<CodeBlock language="tsx" live scope={{Button}}>
  {`<Button variant="tertiary">Tertiary button</Button>`}
</CodeBlock>

## Accessibility

The `Button` component has a `role="button"` property provided. Ensure that the element has the appropriate role for its use:

* `role="submit"` for a form submission button
* `role="reset"` for a form reset button

#### Related

* ["Building Accessible Buttons with ARIA" on Deque ↗](https://www.deque.com/blog/accessible-aria-buttons/)
* ["ARIA: button role" on MDN ↗](https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Roles/button_role)

## Component API

### Props

<PropsTable component={Button} />

### Methods

<MethodsTable component={Button} />