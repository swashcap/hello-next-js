import {Breadcrumb} from '../../components/library/Breadcrumb'
import {CodeBlock} from '../../components/styleguide/CodeBlock'
import {Grid} from '../../components/styleguide/Grid'
import {MethodsTable} from '../../components/styleguide/component/MethodsTable'
import {PropsTable} from '../../components/styleguide/component/PropsTable'
import {TableOfContents} from '../../components/styleguide/component/TableOfContents'

# Breadcrumb

<Grid>
  <Grid.Column className="w-100 w-75-l">
    <p className="f4">
      Give users context to their current location in a hierarchical navigation system with Breadcrumb.
    </p>
    <p>
      The breadcrumb provides awareness in deeply nested page structures or views, and permits users to move freely up the structure, promoting discovery.
    </p>
  </Grid.Column>
  <Grid.Column className="w-100 w-25-l">
    <TableOfContents
      items={[
        ['Usage'],
        ['Accessibility'],
        ['Component API', ['Props', 'Methods']]
      ]}
    />
  </Grid.Column>
</Grid>

#### Related

* [Navigation design guidelines ↗](#)

## Usage

Import the `Breadcrumb` component into a React component in your project:

```typescript
import {Breadcrumb} from 'hello-next-js'
```

The root `Breadcrumb` is a styling wrapper for the breadcrumb's items, exposed through the `Breadcrumb.Item` component. Compose these in a React component in your project to form a breadcrumb:

<CodeBlock language="tsx" live scope={{Breadcrumb}}>
  {`<Breadcrumb>
  <Breadcrumb.Item href="/music">Music</Breadcrumb.Item>
  <Breadcrumb.Item href="/music/hip-hop">Hip Hop</Breadcrumb.Item>
  <Breadcrumb.Item active>Wu-Tang Clan</Breadcrumb.Item>
</Breadcrumb>`}
</CodeBlock>

## Accessibility

Ensure that `Breadcrumb.Item`s have the appropriate `href` property to ensure the links are accessible:

```tsx
<Breadcrumb.Item href="/path/to/sub-page">Sub Page</Breadcrumb.Item>
```

The `Breadcrumb` component applies `aria-current="page"` to a `Breadcrumb.Item` with an `active` property.

#### Related

* [WAI-ARIA 1.1 `aria-current` spec ↗](https://www.w3.org/TR/wai-aria-1.1/#aria-current)

## Component API

### Props

<PropsTable component={Breadcrumb} />

### Methods

<MethodsTable component={Breadcrumb} />
